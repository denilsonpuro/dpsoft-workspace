# Agents

Agents are tenant-owned configurations with explicit connector and tool assignments. Model providers will implement a provider-neutral interface. Tool results carry provenance and are treated as data, never as trusted instructions.
