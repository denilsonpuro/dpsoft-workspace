# Workflows

Workflow persistence and execution are scheduled after the vertical PostgreSQL-to-chat milestone. Planned nodes include triggers, MCP tools, conditions, and human approvals. Durable background execution will use an idempotent queue rather than long HTTP requests.
