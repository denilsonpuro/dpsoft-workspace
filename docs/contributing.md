# Contributing

Keep tenant scope explicit, route every tool call through the registry, add denial-path tests for authorization changes, and avoid UI claims that are not backed by a live service. Run `npm run check` before review.
